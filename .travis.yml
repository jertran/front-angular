language: 
  node_js
node_js:
  - "0.10"
before_install:
  - npm i -g npm@5.8.0
deploy:
  local-dir: "town"
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  on:
    branch: master